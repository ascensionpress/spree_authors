<%= form_for(@spree_author) do |f| %>
  <% if @spree_author.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spree_author.errors.count, "error") %> prohibited this spree_author from being saved:</h2>

      <ul>
      <% @spree_author.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :biography %><br />
    <%= f.text_area :biography %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :permalink %><br />
    <%= f.text_field :permalink %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
